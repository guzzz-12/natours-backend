extends base
block content
  main.main
    if subtitle && tours.length > 0
      h2.heading-secondary(style="width: 100%; text-align: center; margin-bottom: 1.5rem")= subtitle
    if subtitle && tours.length === 0
      h2.heading-secondary--error(style="width: 100%; text-align: center; color: #fff") You don't have bookings yet. 
    div.card-container
      each tour in tours
        include _tourCardTemplate